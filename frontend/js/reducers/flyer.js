
const initialState = {
  author: '',
  widgetsArray: [],
};

const flyer = (state = initialState, action) => {
  const { type } = action;

  switch (type) {
    default:
      return state;
  }
};

export default flyer;
